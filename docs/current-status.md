# 观鸟 Web App｜当前状态

> 进入项目的新 ChatGPT 对话或 Codex 线程时，请先阅读本文件，再阅读 `project-overview.md`、`requirements.md` 和 `mvp-scope.md`。

## 当前阶段

产品需求、MVP 范围、主要技术路线和 B′ 完整 MVP 高保真页面实现基线已经确认。Rails 本地初始化和数据库基础验证已经完成；静态 Rails 前端的 14／14 个计划页面或流程状态及阶段 1～7 已全部通过用户验收。后端阶段 1～5 已完成；阶段 6“可登录的空观察历史”中，M1 与 M2-A～C 已完成实现和机械验证，目前等待用户验收完整账户闭环及中日文页面反馈。

注册、统一重发、两步邮箱验证、手动登录、受保护空观察历史、设置、语言切换和退出已经接入正式 Rails 路由、Controller、View、Session 与五张账户业务表。自动测试通过不能替代用户对页面行为和可见反馈的验收，因此第一个纵向切片仍标记为“待验收”，不能写成阶段 6 已完成。精确路径、内部 Cookie／Session 键名、确认结果的 Post／Redirect／Get 方式、当前微文案和维护类／任务名称属于当前工程实现，不自动成为不可调整的产品或 UI 决定。

静态前端实施与验收进度由 `frontend-development-plan.md` 跟踪。当前用户已验收 14／14 个页面或流程状态，已有基础批次及阶段 1～7 全部完成。这里的阶段是静态前端交付批次，不代表后续业务纵向切片的边界或顺序已经确认。

已完成的前端批次只包含静态 Rails 页面、双语、局部交互、代表性错误效果、可访问性和多尺寸回归，不接入业务数据持久化，也不实施真实认证、服务器校验或后端业务逻辑。因此，静态前端完成不等于后端 MVP 或完整 MVP 已完成。

`frontend-implementation-guide.md` 已建立为当前 Rails 前端实现、维护和学习入口，集中记录代码组织、维护约定、验证方式及高保真基线与 Rails 实现差异；阶段进度仍由 `frontend-development-plan.md` 负责。

后端开发采用“设计整体、按纵向切片实施”的方式。`backend-development-plan.md` 负责后端阶段、依赖、状态、验证证据与进度；`database-design.md` 负责领域模型、ER 图、物理 Schema、约束与 Migration 映射。在第一个业务切片前先完成后端边界、领域模型、ER 图和物理数据库设计基线，但不会一次性创建全部业务表，Migration 按实际纵向切片逐步实施。

从后端阶段 2 开始，常规、低成本且可逆的工程决定由开发 Agent 根据正式文档、Rails 常规实践和项目约束自主完成；关键决定按 `AGENTS.md` 升级给用户。阶段 2～5 已在授权范围内完成交付；用户主要验收业务行为并在重要纵向节点理解核心 Rails 请求链路。

`database-design.md` 的完整 MVP 10 实体设计未变，不是用户逐字段确认或全部实现。M2 完成复核时，开发／测试两库仍各有 5 张业务表：`users`、`sessions`、`email_verification_tokens`、`verification_rate_limit_keys`、`verification_send_attempts`，且均无残留数据；M2-B／C 没有新增 Migration 或业务表。M2-A 的建表与 Schema 往返、M2-B 的注册／邮件故障验证、M2-C 的正式页面与维护清理证据分别见 `backend-development-plan.md` 12.6～12.8 节；观察、识别和密码辅助结构仍待后续批次。

## 已确认

- 产品是面向普通用户、移动端优先的观鸟 Web App；
- 核心用户主要使用望远镜，没有照片或不希望依赖照片识别；
- 核心问题是保存会迅速消失的颜色、花纹和局部视觉印象；
- 第一阶段优先覆盖福冈日常、静态或低速活动中的鸟；
- 第一版以结构化输入为主；
- 轮廓采用两层纯视觉选择，第一层约 4 个大组、第二层约 12～14 个具体轮廓，并提供“没有合适的轮廓”兜底项；
- 第一版可记录部位为头部、胸腹、翅膀和尾部；
- 每个部位均为可选；
- 编辑页默认显示头部面板，但未填写有效内容时不产生头部数据；
- 每个部位支持一个主颜色和一个可选补充色；
- 每个部位最多选择一个预设花纹或特征标签，其他细节使用自由文字补充；自由文字进入摘要但不影响抽象图；
- 确定程度采用三档：确定、大概如此、非常模糊；
- 用户完成一个部位编辑或切换部位后，系统统一更新抽象鸟类印象图和文字摘要；抽象图只通过结构化输入修正；
- 新建观察记录只有在选择普通轮廓或兜底轮廓，并且至少一个身体部位记录有效视觉特征后才能保存；
- 第一版不引入草稿状态、草稿页面或自动保存；
- 有未保存修改时离开编辑流程必须二次确认继续编辑或放弃修改；
- 可选补充最多两个行动位置标签和行为自由文字；不设置手动观察时间、独立地点、环境或普通备注字段；
- 鸟种识别状态为待确认、候选中和已确认，根据候选与最终鸟名自动计算；最多保存三个自由输入的候选鸟名；
- 鸟种识别操作按数据丢失风险区分：添加候选和确认最终鸟名直接生效，删除候选和修改最终鸟名需要保存与未保存离开保护；非空未提交鸟名文字也需要离开保护；撤销确认时，非候选最终鸟名是否保留由用户选择，候选已满时由用户明确选择替换项；已确认状态仍保留候选鸟名；
- 第一版采用邮箱和密码账户，注册后必须验证邮箱再手动登录，并支持邮件链接重置密码和登录后修改密码；
- 密码为 8～20 个字符，只允许 ASCII 英文字母和数字，且至少包含一个英文字母和一个数字；
- 新验证令牌保存且邮件任务实际入队成功后只有最新链接有效，链接成功使用后立即失效；入队失败保留仍有效的旧链接，入队后投递失败通过重试或重发恢复、不恢复旧链接；入队不等于送达，用户已接受开发进程退出可能丢失内存邮件任务的限制；
- 验证邮件重发限制为同邮箱至少间隔 60 秒、同邮箱 15 分钟最多 3 次、同 IP 15 分钟最多 10 次；两个重发入口共用流程与统计，且反馈不泄露邮箱是否存在；
- 重复注册不创建第二个账户，也不由注册动作自动重发验证邮件；注册已创建账户但邮件暂未发出时保留未验证账户，并允许通过统一重发流程恢复；
- 邮箱验证采用两步服务器流程：打开链接只检查令牌并显示脱敏邮箱确认页，用户确认时再次校验并原子完成验证与令牌失效；邮件扫描器仅打开链接不会验证账户；
- 忘记密码重置链接有效期为 30 分钟，只有最新链接有效且只能使用一次；重置成功结束全部会话，登录后修改密码成功保留当前会话并结束其他会话；
- 注册、验证邮件已发送、重新输入邮箱发送和登录采用相对独立页面；真实结果由服务器判断，字段错误、页面级通用提示和结果卡片分别承担可修正输入、整体错误和流程结果反馈；
- 邮箱不存在或密码错误使用同一登录失败提示；凭证正确但账户未验证时不创建会话，并提供重新验证入口；登录后的返回地址只允许有效站内相对路径；
- 登录状态最长保留 30 天；退出只立即结束当前浏览器的当前会话；MVP 不提供“记住我”；
- 观察历史优先显示抽象图缩略图和识别状态；候选中不显示候选名，已确认显示最终鸟名；
- 当前低保真原型包含登录、注册、观察历史、轮廓选择、记录编辑、记录详情和设置七个核心页面，以及“08 账户辅助流程”状态集合；该辅助流程顶部只保留忘记密码和修改密码，邮箱验证、设置新密码与结果反馈归入各自流程内部；
- 底部导航包含观察历史、新建观察和设置，只在观察历史、观察详情和设置页显示；
- MVP 显示语言为简体中文和日本語；主动选择只保存在当前浏览器并立即生效；
- 未保存语言偏好时，所有 `zh*` 浏览器语言使用简体中文，其他语言使用日文；已保存的用户选择优先；英文显示延后；
- 设置页面包含当前邮箱、修改密码、显示语言和退出登录；
- Ruby on Rails 仍是主要后端学习与开发方向。

## 已确认技术方向

- 使用 Windows 原生开发环境，项目位于 `E:\AAproject\birding-app`；已安装并验证 Ruby 4.0.6、Rails 8.1.3.1 和 MySQL 8.4.10；
- 保留现有 MySQL 5.7 服务及数据；MySQL 8.4.10 已作为独立服务 `MySQL84` 在 `127.0.0.1:3307` 运行；
- Rails 使用非 root 账号 `birding_app`，只拥有开发库和测试库相应权限；密码通过 Windows 用户环境变量提供，不写入 Git；
- Windows 下 `mysql2` 已成功编译并连接 MySQL 8.4；数据库字符集与排序规则已验证为 `utf8mb4`／`utf8mb4_0900_ai_ci`；
- 采用 Rails 模块化单体、Rails Views、Turbo、Stimulus 和少量普通 JavaScript，MVP 不采用 Vue、独立 SPA、独立前端仓库或纯 API 后端；
- 抽象图使用服务器生成的 inline SVG，并通过 Turbo Frame 与 Stimulus 渐进预览；只持久化结构化观察数据；
- 采用 Rails 8 第一方认证、`has_secure_password`、数据库 Session、Rails 加密 Cookie 和当前用户作用域授权；令牌摘要、Session 不保存 IP／User-Agent、限流带密钥摘要及数据保留边界已确认，详见 `technical-decisions.md` 6.8 节；
- 邮箱验证链接在 15 分钟内有效并采用确认页二次提交；密码重置链接在 30 分钟内有效、只有最新链接有效且只能使用一次；验证邮件支持注册结果页直接重发和重新输入邮箱请求发送；
- 使用 Rails I18n；`zh*` 浏览器语言映射为简体中文，其他语言回退到日文，语言偏好只保存在当前浏览器；
- 使用 Minitest 测试体系和本地运行、部署准备、公开部署三阶段交付；
- 第一个纵向切片确认为“可登录的空观察历史”。

## 已完成的基础、静态前端与后端分批验证

以下区分历史基线与 M1、M2-A～C 开发记录；本次文档维护只核对开发 Agent 的记录和当前代码，未重新执行数据库命令或 Rails 测试。

- Rails 项目已在仓库根目录初始化，开发库与测试库已准备，`/up` 健康检查通过；
- Rails 初始化基线测试：1 个测试、1 个断言，0 失败、0 错误、0 跳过；
- 静态前端最终集成测试：14 个测试、440 个断言，0 失败、0 错误、0 跳过；
- 静态前端最终 System Test：13 个测试、312 个断言，0 失败、0 错误、0 跳过；
- 后端阶段 4 的 2026-08-31 本地复测：相同 TLS 配置在获批准的非沙箱环境连接两库成功；普通 Rails 测试 15 个／441 断言、System Test 13 个／312 断言，合计 28 个／753 断言，0 失败、错误或跳过。证据与命令见 `backend-development-plan.md` 10.4 节，不代表已覆盖认证、限流、业务 Schema 或并发行为；
- 阶段 4 RuboCop：33 个文件、0 个违规；Brakeman：79 项检查、0 个错误、0 个安全警告；
- 阶段 6／M1 开发验证记录：普通 Rails 测试 48 个／666 断言（其中 33 个为本批新增），既有静态前端 System Test 13 个／312 断言，合计 61 个／978 断言，0 失败、错误或跳过；空测试库迁移回滚／重建与 Schema 往返通过，RuboCop 46 个文件无违规，Brakeman 79 项检查无错误或安全警告。命令、首轮失败及修正、并发重复验证和覆盖缺口见 `backend-development-plan.md` 12.5 节；不代表正式账户页面或整个切片已验收；
- 阶段 6／M2-A 开发验证记录：普通 Rails 测试 85 个／957 断言（本批新增 37 个），既有 System Test 13 个／312 断言，合计 98 个／1269 断言，0 失败、错误或跳过；三表迁移回滚／重建及五表 Schema 全新加载往返通过，RuboCop 60 个文件无违规，Brakeman 79 项检查无错误或安全警告。并发重复测试单独计数，完整命令、失败修正与覆盖边界见 `backend-development-plan.md` 12.6 节；不代表邮件流程或正式验证页面已完成；
- 阶段 6／M2-B 开发验证记录：注册、真实令牌轮换、邮件任务、孤立 Job、并发重发、worker 状态重检、队列载荷与隐私日志已经完成机械验证；普通 Rails 测试 109 个／1114 断言、System Test 13 个／312 断言，均为 0 失败、错误或跳过。完整证据见 `backend-development-plan.md` 12.7 节；
- 阶段 6／M2-C 开发验证记录：正式账户页面、完整浏览器闭环、CSRF、安全返回路径、语言切换和维护清理已经完成机械验证；普通 Rails 测试 120 个／1259 断言、System Test 14 个／330 断言，合计 134 个／1589 断言，0 失败、错误或跳过；五轮关键并发复测、Zeitwerk、RuboCop 与 Brakeman 均通过。开发／测试库五张账户业务表收尾时各为 0 行，MySQL 8.4.10 与既有 TLS cipher 未改变。证据见 `backend-development-plan.md` 12.8 节；阶段 6 仍等待用户验收；
- 静态前端已完成 320px、390px、1440px、长日文、键盘顺序和短视口代表性回归；真实移动设备、软键盘及 Safari、Firefox 等多浏览器验证尚未完成；
- MySQL 5.7 全量备份已生成：`E:\MySQL57-backups\20260810-161600\mysql57-all.sql`，大小 1,198,910 字节，交接记录中的 SHA-256 摘要为 `ECD5…893`；已检查 dump 标记，尚未进行恢复演练。

## 延后确认与验证

密码规则、验证邮件旧链接失效、重发限制和浏览器语言归类已经确认，不再属于待讨论项。

阶段 2～5 已交付，M1 与 M2-A～C 没有遗留工程阻塞。旧 TLS 失败在沙箱执行上下文仍可复现，但相同配置在获批准的正常本地环境已完成两库迁移及阶段 6 当前回归。本轮没有关闭证书验证、换用 Oracle 客户端或修改 TLS 配置；也不表示这些备选方案已被用户否决。本地连接加密的证据不能证明生产 CA／主机身份验证已经完成，生产 TLS 与部署配置仍延后处理。

仍需保留：

- M2-A／B／C 只是已完成的工程实施分组，不是新的产品模块；机械验证与维护清理已经完成，但完整账户闭环和中日文可见反馈仍待用户验收；
- 当前本地维护任务已经覆盖过期 Session 次日清理、失效或长期过期验证令牌 7 天保留、限流尝试 24 小时保留及孤立限流键清理；正式调度方式仍留到部署阶段；
- 后续纵向切片的依赖与具体顺序可由开发 Agent 在既有授权边界内细化；鸟种识别与密码辅助的先后仍可调整；
- MySQL 5.7 备份的隔离恢复演练时机；
- MySQL 8.4 旧初始化目录和安装文件是否清理；
- 生产托管平台、生产 MySQL、真实邮件供应商、发件域名、Secrets、CI、生产备份、监控和告警方案；
- 正式轮廓、颜色、花纹／特征和行动位置名单，以及其余 UI 实现细节。

## 当前 UI 实现基线

- 项目级 `$project-ui-designer` 负责 UI/UX、页面流程、原型制作与验证，并在产生实质性 UI 决定时输出一次性交接单；它不直接修改正式项目文档；
- UI 视觉任务由 `$project-ui-designer` 先使用 `$frontend-design` 建立视觉方向，再使用 `$mobile-app-ui-design` 检查移动端体验；
- B′“柔雾标本册”已经确认为正式页面视觉方向；
- `prototype/experiments/high-fidelity-baseline.html` 是整个 MVP 的正式高保真实现基线，覆盖登录、注册、账户辅助流程、空与非空观察历史、轮廓选择、观察编辑、观察详情、设置、修改密码、设计系统和 Rails 前端组件参考；
- 第一个纵向切片需要的注册、邮箱验证、手动登录和空观察历史已经具有高保真页面参考；
- `prototype/assets/app-logo-preview.png` 中的第三版 Logo 已确认为正式 App Logo，并已用于高保真登录页；前两版不再是正式方案；
- 正式前端开发无需等待所有后续 UI 细节全部确认；
- 14／14 个计划页面或流程状态已经完成静态 Rails 实现并通过用户验收；该结论只适用于静态前端阶段；
- 第一个纵向切片的正式账户页面已接入真实后端并通过机械验证，但完整账户闭环与中日文页面反馈尚未通过用户验收；
- 实现时可以根据浏览器实际效果进行必要适配，但不得擅自改变整体视觉方向；
- 原型中的示例鸟名、轮廓名称、颜色名称、具体文案和演示记录不因视觉基线获确认而成为正式业务数据；
- 原型中的每一项微交互不自动成为不可调整的最终规格；
- `prototype/experiments/visual-direction-comparison.html` 保留为 B′ 形成过程中的视觉实验记录，不再承担正式实现基线职责。

## 待专项验证与 UI 细化

- 第一层视觉大组与第二层具体轮廓的最终名单、名称、排序和福冈常见鸟覆盖；
- 约 10～12 个基础颜色家族的最终名单和显示色值；
- 通用及身体部位专属花纹或特征标签的最终集合；
- 行动位置标签的最终集合；
- 忘记密码、设置新密码、重置成功和修改密码的最终路由或状态拆分，以及真实服务器异常反馈的最终文案、提交后焦点和字段值保留方式；
- 保存条件提示、候选折叠入口、撤销确认弹窗的最终文案与视觉表现，以及部位确定程度的最终控件与排版；
- 底部导航最终图标、新建入口表现和设置页语言控件；
- 真实移动设备、移动端软键盘、Safari、Firefox 等多浏览器环境和具体微交互的最终适配；
- 正式 Logo 的页面与邮件应用位置、尺寸、安全边距、投影、响应式适配和衍生图标。

## 当前不做

- 自动推荐具体鸟种；
- 地区、季节和大规模分布数据库；
- 自动抓取网络昵称、梗和表情包；
- AI 自动搜索和整理网络文化内容；
- 用户投稿及内容审核；
- 社交功能；
- 复杂 AI 生图；
- 高自由度绘图工具；
- 专门的飞行鸟记录系统；
- 语音录音、语音转文字和自然语言解析；
- 自由文字自动转换为图形；
- 草稿与自动保存；
- 用户名登录、第三方 OAuth 和修改邮箱；
- 鸟种识别“确认依据”文字字段；
- 微信小程序；
- Android 或 iOS 原生 App；
- 正式发布的 LINE MINI App；
- 一次性覆盖所有鸟类知识和识别能力。

## 下一步

当前后端阶段 6 等待用户验收：

- 请用户在本地验收“注册 → 验证 → 手动登录 → 空观察历史 → 退出”完整闭环及中日文页面反馈；机械验证通过不替代这一步；
- 验收通过后再把阶段 6 标记为完成，并按当前暂定顺序进入阶段 7 Observation 核心闭环；阶段 7 以后的具体顺序仍可依据依赖调整；
- Migration 继续随切片实施，不一次性创建全部业务表；当前仍只有五张账户业务表，没有 Observation 或其他后续业务表；
- 本地数据库验证继续限定开发／测试库与原有连接配置；遇到同类沙箱 TLS 错误时先做获批准的执行上下文对照，不自行改变安全策略；
- 后续 Observation 首次可保存时必须已具备有效 PartImpression，不能先交付仅轮廓保存；鸟名 255 字符、部位描述和行为各 2000 字符，超长报错不截断，详见 `requirements.md`；
- 按已确认的密码、验证邮件、限流、语言和账户反馈规则接入真实后端；
- 为账户、授权和观察历史业务行为补充测试，继续按开发 Agent 的测试报告进行验收；
- 继续确定后续纵向切片的边界与顺序；
- 接入真实后端时继续使用已确认的高保真基线和静态 Rails 页面作为前端参考，但不得把查询参数状态、示例数据或浏览器内存状态直接当作业务实现；
- 用大濠公园等福冈常见鸟样本验证轮廓覆盖；
- 确定基础颜色、花纹或特征及行动位置标签的最终名单；
- 继续细化两层轮廓、识别状态、补充观察信息，以及账户辅助流程的异常反馈和最终页面拆分；
- 在实现过程中继续确认账户异常状态、页面拆分、核心控件、移动端适配和 Logo 应用细节。

后续正式实施由开发 Agent 继续。账户、语言和 UI 未决项已经进入静态前端完成后的下一轮讨论清单；生产与部署事项仍延后到本地 MVP 闭环验收之后。
