# 观鸟 Web App｜数据库设计

> 文档性质：MVP 领域模型、ER 图与物理数据库设计文档  
> 建立日期：2026-08-17  
> 当前状态：结构已建立，阶段 1 后端设计尚未开始，ER 图与物理 Schema 尚未确认  
> 实施计划：[backend-development-plan.md](backend-development-plan.md)

## 1. 文档职责

本文档负责：

- MVP 领域对象、关系、基数和生命周期；
- 概念 ER 图与逻辑数据模型；
- MySQL 物理表、字段、类型、长度、默认值与可空性；
- 主键、外键、唯一约束、检查约束和索引；
- Rails Validation 与数据库约束的职责分工；
- 删除策略、事务、并发和数据完整性；
- 敏感数据、令牌、Session、限流和日志安全；
- 数据库设计到 Migration 切片的映射；
- 数据库相关未决问题与已确认设计记录。

本文档不负责：

- 决定产品能力或业务规则；
- 决定 MVP 是否纳入某项功能；
- 记录 UI 页面和交互细节；
- 记录开发阶段进度和测试计数；
- 把原型示例、静态前端状态或 AI 建议升级为正式数据结构。

发生冲突时，依次参考：

- 产品能力和业务规则：[requirements.md](requirements.md)
- MVP 范围和完成标准：[mvp-scope.md](mvp-scope.md)
- 技术路线：[technical-decisions.md](technical-decisions.md)
- 后端阶段计划：[backend-development-plan.md](backend-development-plan.md)
- UI 与页面状态：[ui-design.md](ui-design.md)

## 2. 信息状态

| 状态 | 含义 |
| --- | --- |
| `confirmed` | 用户已确认，或当前正式文档已有明确决定 |
| `provisional` | 当前暂定采用，仍需实施或验收验证 |
| `open-question` | 尚未确认，不得直接写成正式 Schema |
| `deferred` | 当前阶段或当前 MVP 不处理 |
| `rejected` | 已明确不采用 |
| `observed-only` | 只出现在代码、原型或示例中 |
| `AI-suggested` | 仅由 AI 提出，尚未确认 |

表名、字段名、类型、长度、索引名和删除行为只有在物理设计阶段得到确认后，才能标记为 `confirmed`。

## 3. 已确认的数据库技术边界

以下内容来自当前正式技术决定，状态为 `confirmed`：

- 数据库使用 MySQL 8.4.10 LTS；
- 开发库和测试库分离；
- 使用 InnoDB、MySQL strict mode、`utf8mb4` 和 `utf8mb4_0900_ai_ci`；
- 数据库结构由 Rails Migration 管理，并使用 Rails 默认 `schema.rb`；
- 核心业务数据使用关系结构；
- 不使用 MySQL ENUM、触发器、存储过程或核心 JSON 大字段；
- MVP 不引入软删除、审计表或版本历史表；
- 重要规则同时使用 Rails Validation 和必要的数据库非空、唯一、外键与索引约束保护；
- 个人 Observation 必须属于 User，并从当前用户关联集合中访问；
- 轮廓、颜色、花纹／特征、行动位置与 SVG 映射使用版本控制配置和稳定键，不建立 MVP 字典表或管理后台；
- SVG、文字摘要和鸟种识别状态为派生结果，不单独持久化；
- locale 偏好保存在签名持久化浏览器 Cookie 中，不写入 User；
- 数据库存储 UTC 时间，界面按 `Asia/Tokyo` 显示。

## 4. 数据分类基线

### 4.1 持久化业务数据

当前已确认需要持久化的概念数据：

- User 的账户身份、密码摘要和邮箱验证状态；
- 登录 Session；
- Observation 与 User 的归属；
- Observation 的轮廓稳定键、正式记录时间、行为文字和最终鸟名；
- 每个实际记录身体部位的结构化印象；
- 候选鸟名；
- 行动位置选择。

邮箱验证令牌、密码重置令牌和限流统计是否作为独立持久化对象，仍需在领域模型阶段确认。

### 4.2 版本控制配置

以下内容不建立数据库字典表：

- 视觉大组和具体轮廓；
- 基础颜色；
- 花纹或特征；
- 行动位置；
- SVG 轮廓与部位映射；
- 本地化显示名称。

业务表只保存稳定键。正式键名、名单和版本演进方式尚未确认。

### 4.3 派生数据

以下内容不单独持久化：

- 待确认、候选中和已确认识别状态；
- 文字特征摘要；
- 生成后的 SVG 文件或字符串；
- 历史页抽象图缩略图。

它们应根据已保存结构化数据实时计算或渲染。

### 4.4 浏览器状态

以下内容不写入业务数据库：

- 用户主动选择的界面语言；
- 未提交的表单文字；
- 当前展开或折叠的候选区域；
- 当前编辑中的临时选择；
- 静态预览使用的查询参数状态。

正式业务提交后，服务器持久化结果才是事实来源。

## 5. 当前业务模块候选

以下模块划分将在后端设计阶段确认，当前状态为 `provisional`：

| 模块 | 当前职责候选 | 数据相关关注点 |
| --- | --- | --- |
| 账户身份 | 邮箱、密码摘要、验证状态 | 邮箱规范化、唯一性、密码规则 |
| 登录会话 | 登录、30 天到期、当前会话退出 | Session 持久化、Cookie、失效时间 |
| 邮箱验证 | 15 分钟令牌、最新链接、一次使用 | 令牌摘要、轮换、发送失败事务边界 |
| 重发限流 | 邮箱与 IP 时间窗口 | 持久化方式、并发计数、IP 隐私 |
| 密码辅助 | 重置密码、修改密码 | 重置令牌、其他 Session 是否失效 |
| 观察记录 | 创建、保存、历史、详情和编辑 | User 归属、首次保存时间 |
| 部位印象 | 四个身体部位的结构化记录 | 每个部位唯一、有效内容判断 |
| 鸟种识别 | 候选、最终鸟名、撤销和修改 | 最多三个候选、派生状态 |
| 补充信息 | 行动位置和行为文字 | 最多两个行动位置 |
| 表现生成 | SVG 和文字摘要 | 只读取结构化数据，不持久化结果 |

该表只用于阶段 1 讨论，不构成已确认模块边界。

## 6. 概念模型基线

当前正式技术文档记录的概念关系如下，状态为 `confirmed`，但不是最终 ER 图：

```text
User
├─ has_many Session
└─ has_many Observation
   ├─ has_many PartImpression
   ├─ has_many BirdCandidate
   └─ has_many ActivityLocationSelection
```

概念职责：

| 对象 | 当前已确认的概念职责 |
| --- | --- |
| User | 邮箱、密码摘要、邮箱验证状态和账户时间信息 |
| Session | 属于 User 的数据库登录会话 |
| Observation | 属于 User，保存观察级结构化数据 |
| PartImpression | 属于 Observation，每个实际记录部位最多一条 |
| BirdCandidate | 属于 Observation，保存自由输入候选鸟名 |
| ActivityLocationSelection | 属于 Observation，保存行动位置稳定键 |

邮箱验证、重发限流和密码重置相关对象是否加入 ER 图，仍是 `open-question`。

## 7. ER 图

**当前状态：`未开始`**

本节将在后端开发计划阶段 2 中完成。ER 图至少需要表达：

- 实体；
- 一对一、一对多或其他基数；
- 必选与可选关系；
- 聚合根；
- 所有权和生命周期；
- 删除传播方向；
- 派生状态与持久化状态的分界；
- 仍未确认的候选实体。

在 ER 图确认前，不得依据本节生成 Migration。

## 8. 逻辑模型待设计清单

| 概念对象 | 是否确认存在 | 关系是否确认 | 逻辑属性是否完成 | 备注 |
| --- | --- | --- | --- | --- |
| User | 是 | 部分 | 否 | 验证令牌建模待确认 |
| Session | 是 | 是 | 否 | 到期与失效字段待设计 |
| Observation | 是 | 是 | 否 | 正式记录时间字段待设计 |
| PartImpression | 是 | 是 | 否 | 部位唯一性与有效数据规则待设计 |
| BirdCandidate | 是 | 是 | 否 | 最多三个与并发写入待设计 |
| ActivityLocationSelection | 是 | 是 | 否 | 最多两个与稳定键待设计 |
| 邮箱验证相关对象 | 未确认独立存在 | 否 | 否 | 可能属于 User 字段或独立对象 |
| 验证邮件限流对象 | 未确认独立存在 | 否 | 否 | 数据库或其他持久化方式待确认 |
| 密码重置相关对象 | 未确认独立存在 | 否 | 否 | 产品规则仍有未确认项 |

## 9. 物理表清单

**当前状态：`未开始`**

物理设计确认后使用以下表格：

| 表 | 职责 | 对应领域对象 | 所属切片 | 状态 |
| --- | --- | --- | --- | --- |
| 待设计 | 待设计 | 待设计 | 待设计 | `open-question` |

当前概念对象名称不自动等于最终表名。

## 10. 数据字典模板

每张表使用同一模板，避免只列字段而不说明理由。

### 10.1 表职责

- 对应领域对象：
- 数据所有者：
- 创建时机：
- 更新时机：
- 删除或失效时机：
- 主要查询：

### 10.2 字段

| 字段 | 类型 | NULL | 默认值 | 业务含义 | 数据来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 待设计 | 待设计 | 待设计 | 待设计 | 待设计 | 待设计 | `open-question` |

### 10.3 约束与索引

| 类型 | 字段或组合 | 目的 | Rails 层配合 | 状态 |
| --- | --- | --- | --- | --- |
| 待设计 | 待设计 | 待设计 | 待设计 | `open-question` |

## 11. 已确认业务不变量

以下是数据库设计必须支持的业务规则，但具体数据库实现尚未确认：

### 11.1 账户与会话

- 邮箱账户具有唯一身份；
- 密码为 8～20 个 ASCII 英文字母或数字，且至少一个字母和一个数字；
- 未验证邮箱不能登录；
- 邮箱验证链接有效期为 15 分钟；
- 新验证邮件成功后只有最新链接有效；
- 验证链接成功使用后不能重复使用；
- 新邮件发送失败时，仍有效的旧链接继续有效；
- 同邮箱发送至少间隔 60 秒；
- 同邮箱 15 分钟最多重发 3 次；
- 同 IP 15 分钟最多提交 10 次重发请求；
- 两个重发入口共用统计；
- Session 最长保留 30 天；
- 退出只结束当前 Session。

### 11.2 Observation

- 每条 Observation 必须属于一个 User；
- 个人记录必须通过当前用户作用域访问；
- 没有草稿，只有满足最低保存条件的记录才能持久化；
- 正式记录时间为第一次成功保存时间，后续编辑不得改变；
- 行动位置和行为不影响最低保存条件。

### 11.3 PartImpression

- 身体部位限于头部、胸腹、翅膀和尾部；
- 未填写有效内容的部位不产生记录；
- 每个 Observation 的同一身体部位最多一条；
- 有效部位至少包含颜色、预设特征或自由文字之一；
- 每个部位最多一个主色、一个补充色和一个预设特征；
- 每条已记录部位必须有三档确定程度之一。

### 11.4 BirdCandidate 与最终鸟名

- 每个 Observation 最多三个候选鸟名；
- 最终鸟名可以来自候选，也可以是另一个自由输入名称；
- 已确认状态仍保留候选；
- 识别状态根据候选和最终鸟名派生，不直接存储；
- 删除最后一个候选或撤销最终确认后，状态根据剩余数据自动回退。

### 11.5 ActivityLocationSelection

- 每个 Observation 最多两个行动位置；
- 行动位置引用版本控制配置中的稳定键；
- 最终行动位置名单尚未确认。

## 12. Rails Validation 与数据库约束分工

**当前状态：`未开始`**

每项重要规则需要分别判断：

| 规则 | Rails Validation | 数据库约束 | 事务／锁 | 状态 |
| --- | --- | --- | --- | --- |
| 邮箱唯一性 | 待设计 | 待设计 | 待设计 | `open-question` |
| 每个 Observation 每部位最多一条 | 待设计 | 待设计 | 待设计 | `open-question` |
| 最多三个候选 | 待设计 | 待设计 | 待设计 | `open-question` |
| 最多两个行动位置 | 待设计 | 待设计 | 待设计 | `open-question` |
| 最新验证链接与一次使用 | 待设计 | 待设计 | 待设计 | `open-question` |
| 验证邮件时间窗口限流 | 待设计 | 待设计 | 待设计 | `open-question` |

不得仅依靠客户端禁用按钮保证数据库完整性。

## 13. 查询与索引分析

**当前状态：`未开始`**

物理设计阶段至少分析以下查询：

- 按规范化邮箱查找 User；
- 根据登录 Cookie 定位有效 Session 及 User；
- 验证邮箱令牌并检查有效期和是否已使用；
- 统计同邮箱和同 IP 的重发时间窗口；
- 按当前用户和时间倒序读取 Observation 历史；
- 按当前用户和 Observation ID 读取、更新记录；
- 读取 Observation 的部位、候选和行动位置；
- 检查同一部位或同一稳定键是否重复；
- 维护最多三个候选和最多两个行动位置的并发安全。

只有能由真实查询模式解释的索引才应加入物理设计。

## 14. 删除、失效与生命周期

**当前状态：`open-question`**

需要逐项确认：

- User 删除是否属于 MVP；
- User 与 Session 的删除或失效关系；
- User 与 Observation 的删除关系；
- Observation 与 PartImpression、BirdCandidate、ActivityLocationSelection 的删除关系；
- 邮箱验证令牌的轮换、使用和过期表示；
- 密码重置令牌的使用和过期表示；
- 过期 Session、令牌和限流记录的清理方式；
- 是否需要后台任务清理，或按查询忽略后延后物理清理。

在确认前不得默认使用级联删除。

## 15. 安全与隐私

物理设计阶段必须确认：

- 密码只保存摘要；
- 验证与重置令牌不以可直接使用的明文形式持久化；
- Session Cookie 与数据库 Session 记录的对应方式；
- IP 限流记录保存原始地址、二进制值还是不可逆摘要；
- 敏感参数和令牌不得进入普通日志；
- 邮箱存在性不能通过重发结果或耗时差异明显泄露；
- 数据库凭据不提交到 Git；
- 测试环境不发送真实邮件，也不打开邮件浏览器。

以上安全目标中的具体实现仍需设计和验证，不得因为列在本节就视为已完成。

## 16. Migration 切片映射

| 开发切片 | 数据库范围 | 当前状态 |
| --- | --- | --- |
| 纵向切片 1 | User、Session、邮箱验证与重发限流所需结构 | `confirmed` 范围，结构待设计 |
| Observation 核心 | Observation | `provisional` |
| 部位与补充信息 | PartImpression、ActivityLocationSelection | `provisional` |
| 鸟种确认 | BirdCandidate、最终鸟名相关结构 | `provisional` |
| 账户辅助 | 密码重置所需结构 | `provisional`，产品规则仍未完全确认 |

迁移映射只描述分批方向，不代表已经决定表名或 Migration 数量。

## 17. 数据库设计完成标准

### 17.1 领域模型与 ER 图完成标准

- 全部已确认 MVP 能力都有明确的数据归属；
- 实体、关系、基数、所有权和生命周期得到确认；
- 派生、配置、浏览器状态与持久化数据边界清晰；
- 未确认内容保持显式状态；
- 用户确认 ER 图。

### 17.2 物理数据库设计完成标准

- 每张表都有职责、数据字典、主键、外键和索引说明；
- 字段类型、长度、NULL、默认值和敏感性均可解释；
- Rails Validation 与数据库约束分工明确；
- 数量上限、并发、事务、删除和令牌安全得到说明；
- 查询与索引能够相互对应；
- Migration 切片映射完成；
- 用户确认物理设计基线。

## 18. 当前未确认问题

### 18.1 当前业务问题

- 密码重置或登录后修改密码成功时，是否结束其他现存 Session；
- 忘记密码重置链接是否只能使用一次；
- 重新申请密码重置后，旧重置链接是否立即失效；
- “确认依据”文字字段是否进入 MVP；
- 正式轮廓、颜色、花纹／特征和行动位置名单。

### 18.2 当前技术与数据问题

- 邮箱验证令牌属于 User 字段还是独立对象；
- 密码重置令牌属于 User 字段还是独立对象；
- 验证邮件限流使用数据库表还是其他持久化机制；
- 新验证令牌切换与 `deliver_later` 入队的事务边界；
- IP 地址的存储或摘要方式；
- Observation 正式记录时间是否直接使用 `created_at`；
- 最多三个候选和最多两个行动位置如何处理并发写入；
- 删除和过期数据清理策略；
- Windows `mysql2` 与 MySQL 8.4.10 的 TLS 解决方式。

## 19. 设计决定记录

只在用户确认后记录，不把讨论过程或 AI 推荐写成决定。

| 日期 | 决定 | 状态 | 依据 | 影响 |
| --- | --- | --- | --- | --- |
| 2026-08-17 | 先完成整个 MVP 的领域与物理数据库设计基线，再按纵向切片实施 Migration | `confirmed` | 用户确认的后端开发流程 | 数据库设计先于第一个业务切片 |
| 2026-08-17 | 开发计划与详细数据库设计分为两份文档 | `confirmed` | 用户确认两层文档结构 | 计划管理进度，本文档管理 ER 与 Schema |

## 20. 当前下一步

数据库设计暂不直接进入字段和索引讨论。先完成后端开发计划阶段 1：

1. 确认业务模块边界；
2. 确认主要请求与数据流；
3. 确认授权边界；
4. 确认持久化、派生、配置和浏览器状态分类；
5. 汇总进入 ER 图前必须解决的问题。

